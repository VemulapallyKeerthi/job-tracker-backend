# ── Database (Supabase) ───────────────────────────────────────────────────────
# Get this from: Supabase → Project → Settings → Database → URI
# Local SQLite fallback (no setup needed for local dev):
# DATABASE_URL=sqlite:///./jobs.db

# Production Supabase:
DATABASE_URL=postgresql://postgres:Ve.keer$7282@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ── CORS ──────────────────────────────────────────────────────────────────────
# Local dev:
ALLOWED_ORIGINS=https://job-flow.lovable.app
# Production (your deployed frontend URL):
# ALLOWED_ORIGINS=https://your-frontend.vercel.app

# ── Scraper ───────────────────────────────────────────────────────────────────
# Local:
API_URL=https://job-tracker-backend-k6xs.onrender.com
# Production (your Render backend URL):
# API_URL=https://job-tracker-backend.onrender.com/jobs

SEARCH_QUERIES=data scientist,machine learning engineer,data engineer,software engineer,backend engineer,ML engineer,data analyst,AI engineer,research scientist
# Comma-separated — scraper loops through each location per site
SEARCH_LOCATIONS=Remote,New York NY,San Francisco CA,Seattle WA,Austin TX
HEADLESS=true